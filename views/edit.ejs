<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <title>edit User</title>
</head>
<body class="bg-gray-100 flex justify-center items-center min-h-screen">
  <form action="/edit/<%= user._id %>" method="POST" class="bg-white p-8 rounded-xl shadow-md w-96">
    <h1 class="text-2xl font-semibold text-center mb-6">edit User</h1>

    <div class="mb-4">
      <label class="block text-gray-700 mb-1">Name</label>
      <input type="text" name="name" value="<%= user.name %>" class="w-full px-3 py-2 border rounded-md focus:ring focus:ring-blue-300" required>
    </div>

    <div class="mb-4">
      <label class="block text-gray-700 mb-1">Email</label>
      <input type="email" name="email" value="<%= user.email %>" class="w-full px-3 py-2 border rounded-md focus:ring focus:ring-blue-300" required>
    </div>

    <div class="mb-6">
      <label class="block text-gray-700 mb-1">Image URL</label>
      <input type="text" name="image" value="<%= user.image %>" class="w-full px-3 py-2 border rounded-md focus:ring focus:ring-blue-300">
    </div>

    <button type="submit" class="w-full bg-blue-500 hover:bg-blue-600 text-white py-2 rounded-md font-medium">
      edit
    </button>
  </form>
</body>
</html>
