<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit User</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="relative flex items-center justify-center min-h-screen bg-cover bg-center bg-no-repeat"
      style="background-image: url('https://images.unsplash.com/photo-1503264116251-35a269479413?auto=format&fit=crop&w=1920&q=80');">

  <!-- Dark overlay for contrast -->
  <div class="absolute inset-0 bg-black/40 backdrop-blur-sm"></div>

  <!-- Edit Form -->
  <form action="/edit/<%= user._id %>" method="POST" 
        class="relative z-10 bg-white/20 backdrop-blur-md p-8 rounded-2xl shadow-2xl w-11/12 max-w-md border border-white/30">

    <h1 class="text-3xl font-semibold text-center text-white mb-6">Edit User</h1>

    <div class="mb-4">
      <label class="block text-white/90 mb-1 font-medium">Name</label>
      <input type="text" name="name" value="<%= user.name %>"
             class="w-full px-4 py-2 rounded-lg bg-white/70 text-gray-900 placeholder-gray-500 focus:ring-2 focus:ring-blue-400 focus:outline-none" required>
    </div>

    <div class="mb-4">
      <label class="block text-white/90 mb-1 font-medium">Email</label>
      <input type="email" name="email" value="<%= user.email %>"
             class="w-full px-4 py-2 rounded-lg bg-white/70 text-gray-900 placeholder-gray-500 focus:ring-2 focus:ring-blue-400 focus:outline-none" required>
    </div>

    <div class="mb-6">
      <label class="block text-white/90 mb-1 font-medium">Image URL</label>
      <input type="text" name="image" value="<%= user.image %>"
             class="w-full px-4 py-2 rounded-lg bg-white/70 text-gray-900 placeholder-gray-500 focus:ring-2 focus:ring-blue-400 focus:outline-none">
    </div>

    <button type="submit"
            class="w-full py-2.5 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg shadow-md transition-all">
      Save Changes
    </button>
   </form>
</body>
</html>
