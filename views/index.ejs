<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="relative min-h-screen flex items-center justify-center bg-cover bg-center" 
      style="background-image: url('https://images.unsplash.com/photo-1522202176988-66273c2fd55f?auto=format&fit=crop&w=1920&q=80');">
  
  <!-- Dark overlay -->
  <div class="absolute inset-0 bg-black/60"></div>

  <!-- Form Container -->
  <div class="relative z-10 bg-white/15 backdrop-blur-lg p-8 rounded-2xl shadow-2xl w-full max-w-md mx-4 border border-white/25">
    <h1 class="text-3xl font-semibold text-white mb-6 text-center">User Registration</h1>

    <!-- Registration Form -->
    <form action="/create" method="POST" class="space-y-5">
      <input 
        type="text" 
        name="name" 
        placeholder="Enter name" 
        required 
        class="w-full px-4 py-2 rounded-lg bg-white text-gray-800 placeholder-gray-500 border border-gray-300 focus:outline-none focus:ring-2 focus:ring-indigo-500"
      />

      <input 
        type="email" 
        name="email" 
        placeholder="Enter email" 
        required 
        class="w-full px-4 py-2 rounded-lg bg-white text-gray-800 placeholder-gray-500 border border-gray-300 focus:outline-none focus:ring-2 focus:ring-indigo-500"
      />

      <input 
        type="text" 
        name="image" 
        placeholder="Enter image URL" 
        class="w-full px-4 py-2 rounded-lg bg-white text-gray-800 placeholder-gray-500 border border-gray-300 focus:outline-none focus:ring-2 focus:ring-indigo-500"
      />

      <button 
        type="submit" 
        class="w-full py-2.5 mt-2 bg-indigo-600 hover:bg-indigo-700 text-white font-semibold rounded-lg shadow-md transition-all duration-300"
      >
        Create New User
      </button>
    </form>

    <!-- View All Users Button -->
    <div class="mt-6 text-center">
      <a 
        href="/read" 
        class="inline-block px-5 py-2 bg-gray-800/80 hover:bg-gray-900 text-white font-medium rounded-lg shadow-md 
               border border-white/20 transition-all duration-300">
        👥 View All Users
      </a>
    </div>
  </div>
</body>
</html>
